# CLAUDE.md — ioBroker.homeconnect

> Gemeinsame ioBroker-Wissensbasis: `../CLAUDE.md` (lokal, nicht im Git). Standards dort, Projekt-Spezifisches hier.

## Projekt

**ioBroker Home Connect** — Bosch, Siemens, NEFF und Gaggenau Hausgeräte über die offizielle [Home Connect](https://www.home-connect.com/) Cloud-API (OAuth 2 Device Flow, REST + Server-Sent-Events).

- **Version + Changelog:** aktuelle Version in `io-package.json`; user-facing Changelog: `README.md` + `io-package.json` news (11 Sprachen, handgeschrieben).
- **GitHub:** https://github.com/krobipd/ioBroker.homeconnect — Default-Branch `main` (direkt auf `main` entwickeln, kein Sonderweg). Der `developing`-Zweig im Repo ist ein UNGENUTZTES Überbleibsel (steht 19 Commits zurück) — die CI deckt ihn seit 2026-08-23 trotzdem mit ab, damit ein Push dorthin nicht ungeprüft bliebe.
- **Herkunft:** Greenfield-TS-Neubau, ersetzt den Alt-Code des Community-Adapters (TA2k + Lucky-ESA, `iobroker-community-adapters/ioBroker.homeconnect`). Community-Historie via `git merge -s ours` erhalten; Copyright volle Kette (Memory `reference_copyright_credits_rewrite`). npm-Name gehört bis zu einer möglichen Übernahme dem Community-Paket → Release als Tag + GitHub, **kein npm**. **Version bleibt < 2.0.0**, solange die Zukunft (Bestand-Übernahme vs. eigenständiger Adapter) offen ist.
- **Runtime-Deps:** nur `@iobroker/adapter-core` — HTTP/OAuth/SSE laufen auf Node-22-`fetch` + `AbortSignal.timeout`, kein axios/eventsource.
- **Test-Setup:** vitest (`vitest.config.mts`, im ESLint-`allowDefaultProject`), Tests neben Source unter `src/**/*.test.ts` + `test/standards/` (Repo-Standard-Prüfungen); die Test-Suiten laufen durch den Lint.

## Architektur

```
src/main.ts                     → Adapter-Lifecycle, `I18n.init` (Übersetzungsobjekte für die eigenen Objektnamen), Port-Verdrahtung (AuthController + ApplianceSync + EventStream), **EIN Schreiber für `info.connection`** (= angemeldet UND Strom oben; Strom-401 → `refreshNow`), REST-Transport (apiGet/apiWrite: 401→Refresh, Retry-After-Pause MIT 60s-Fallback ohne Header, Dedup-Log + Recovery-Info, Write-Drop bei Rate-Pause sichtbar; ERWARTETE BSH-Antworten = debug, nie warn — `apiGet` kennt VIER Klassen (Entscheidung 29): **keins** (NoProgramActive/NoProgramSelected, laut Swagger 404 nur auf `/programs/selected` und `/programs/active`) kommt als `null` zurück; **beschäftigt** (WrongOperationState, ProgramNotAvailable — letzteres live auch auf der LISTE `/programs/available`) und **nicht unterstützt** (UnsupportedProgram, ein am Gerät gewähltes Nicht-API-Programm → `sync.noteUnsupportedProgram`) bleiben `undefined` = nichts bekannt; diese drei beweisen den Endpunkt und RÄUMEN eine fehlerhafte Endpunkt-Art (Recovery-Zeile). **Nicht bereit** (`HomeAppliance.Connection.Initialization.Failed`, Gerät gerade eingeschaltet) ist weder Fehler noch Beweis: Dedup unberührt, `sync.noteNotReady` → Abgleich setzt selbst neu an (Entscheidung 30). Jeder Fehlschlag bleibt `undefined`. `apiWrite` bleibt bewusst ohne Klassen — eine abgelehnte Nutzer-Schreibung meldet ihr Ergebnis), `refreshManifestObjects` (die neun `instanceObjects` bekommen bei JEDEM Start Namen+Erklärung aus `admin/i18n` — js-controller wendet das Manifest zwar an, aber mit `preserve` auf `common.name`, eine Umbenennung erreicht sonst nur Neuanlagen; läuft VOR der Zugangsdaten-Prüfung, damit auch eine unkonfigurierte Instanz lesbare Namen trägt; die Kennungen stehen wörtlich am Aufruf, sonst sieht das Flotten-Gate sie nicht), `terminating`-Wächter (onUnload setzt ihn; apiGet/apiWrite/startEventStream UND jeder Schritt der Startkette in `onAuthenticated` steigen aus — sonst synct die fire-and-forget-Startkette am Teardown vorbei, schreibt Objekte nach dem Rückruf und eröffnet den Stream neu → „setTimeout called, but adapter is shutting down"), **die Startkette fängt ihre Fehler selbst** (`try/catch` in `onAuthenticated`, Audit 2026-09-15 F3: sie ist der Rückruf des Anmelde-Controllers — ein durchgereichter Fehler wurde dort als gescheiterte Anmeldung gelesen: „Stored login could not be refreshed … retrying" mit Token-Anfrage je Versuch, im Gerätefluss sogar ein neuer Anmelde-Link; jetzt EINE `error`-Zeile mit dem Schritt, beim Herunterfahren debug), Notification, onStateChange→ApplianceSync **Seit 1.23.0 (Audit 2026-09-24):** lokale Startschritte (Legacy-Aufräumen, Migrationen, Priming, `markAllUnreachable`, `auth.signedIn`=false) einmal in `onReady` VOR der Zugangsdaten-Prüfung; `onAuthenticated` = Abgleich (eigenes `try`) → Abo → Strom, die letzten zwei immer (Nr. 32/33); `armRatePause` = `max()` der Pausen, nach `spaceRequests` erneut geprüft, auch der Verbindungstest und ein 429 des Stroms schalten sie; 401 mit inzwischen gewechseltem Token wiederholt ohne neuen Refresh; eine dauerhafte 4xx-Ablehnung (außer 401/429) → `sync.noteRefused`; `onUnload` wartet über `authCtl.settle()` auf einen laufenden Token-Abruf und über `Promise.allSettled` auf alle Schreibvorgänge.
src/lib/auth-controller.ts      → Anmelde-Lebenszyklus, extrahiert aus main (injizierter AuthPort → unit-testbar): Device Flow inkl. Poll (slow_down verlängert Intervall; **nur ein OAuth-Fehlercode aus RFC 8628/6749 beendet den Code** — Transportfehler, 5xx, unbekannter Code ⇒ derselbe Code wird weiter gepollt, `expiresAt` deckelt den schlimmsten Fall; Audit 2026-09-15 F5: vorher verwarf jeder Blip den Code, den der Nutzer gerade eintippte), Refresh-Timer, invalid_grant beim START UND zur LAUFZEIT → frischer Device-Flow (Link/Notification), abgelaufener/abgelehnter Anmelde-Link erneuert sich selbst, transienter Refresh-Fehler = Retry mit behaltenem Login (warn-once, Backoff 30 s verdoppelnd bis 30 min — Token-Endpunkt-Kontingent 100/Tag; ein Refresh, der erst NACH `stop()` scheitert, bewaffnet keinen Timer und warnt nicht — Audit 2026-09-15 F3) **Seit 1.23.0:** Konfigurationsfehler (`invalid_client`, `unauthorized_client`, `invalid_scope`, `unsupported_grant_type`, `invalid_request`) warnen EINMAL mit Hinweis auf Client ID/Secret und versuchen es nach 5 min erneut (vorher endloser Link-Wechsel); `settle()` gibt den laufenden Token-Abruf an `onUnload`.
src/lib/appliance-sync.ts       → Gerätebaum-Aufbau + Schreibpfad, extrahiert aus main (injizierter AdapterPort → unit-testbar). **Namen:** eigene Texttabelle (Ereignisse) / Cloud-Name (lokalisiert) / abgeleitetes Label / Übersetzungsobjekt je Datenpunkt, `common.desc` = verständliche Erklärung aus `state-texts` (nie der BSH-Schlüssel; ohne Eintrag leer, ein Alt-Bezeichner wird per `null` entfernt), **Reparatur des Altbestands beim Priming** (`refreshLegacyLabels` + `refreshChannelNames`, ohne Cloud-Aufruf), `native.nameSource` merkt die Herkunft (Cloud-Name schlägt abgeleitetes Label; der Adapter-Name gewinnt gegen den DB-Stand, `refreshLabel` einmalig + speichergeführt), Kanäle/Marker/Knöpfe via `tName`; **Schreibpfad typisiert** (`coerceForType` gegen `common.type`, Ack trägt den gewandelten Wert); Cloud-Texte durch `cleanLabel`, native nur Strings. Geräte-Ordner-ID = E-NUMMER vom Typenschild (applianceIdSource: enumber→vib→haId; einmal vergeben, dann via DB gepinnt — App-Umbenennung ändert nur den Anzeigenamen), migrateDeviceIds VOR allem (zieht Alt-Bäume namensbasierter IDs komplett um: Device/Channels/States/Werte/custom, statusStates-Link umgeschrieben; besetzte Ziel-IDs → Serien-Suffix, ohne Typenschild-native wartet der Baum einen Start), Priming der Maps aus vorhandenen Objekten (Offline-Write nach Neustart), Migration umbenannter Datenpunkte VOR dem Priming (misc-Reparatur, Tür-Booleans, programs-Kanal programloser Typen; 1:1 nimmt custom/Name mit; **liest nur den Wert dessen, was umzieht** — die Platz-Prüfung läuft auf einer wertlosen Expansion, denn die Ziel-Ids hängen nicht am Wert; Audit 2026-09-15 F9: vorher ein `getState` je Datenpunkt je Start, 929 auf dem vollen Inventar, für null Ergebnis), Einzel-Definition je Einstellung (`GET /settings/{key}`, Cache in `native.settingDefs` — die Liste trägt keine `constraints`, Entscheidung 27), signaturbasierter Metadaten-Auffrischung beim REST-Sync (metaSignature; **Zusammenführen via extendObject, KEIN Löschen** — Shelly-Modell; `common.states`/`native.bshValues` werden vorab genullt, weil ein Merge nichts entfernt; `custom`/Wert bleiben unangetastet), KEIN Prune (Datenpunkte verschwinden nie — Löschen nur bei Konto-Entfernung), **„nicht bereit“ (Entscheidung 30):** der Durchgang bricht beim ersten `noteNotReady` ab und liest das Gerät nach 30/60/120 s über verwaltete Port-Timer (`setTimer`/`clearTimer`, Muster govee `cloud-retry`) erneut — verworfen bei CONNECTED/PAIRED (frische Stufen), DISCONNECTED, Entfernen und `stop()`, Aufgeben auf debug, ein erfolgreicher Durchgang setzt die Stufen zurück; **Nicht-API-Programme** je haId gemerkt (`unsupportedPrograms`, nur Laufzeit — ein Firmware-Update kann sie freischalten), `syncProgramDefs` fragt sie nicht erneut, Katalog-Events vorab je Gerätetyp, Definitions-Cache je Programm (einmalig laden, persistiert in device native.programOptions), Options-Objekte = Vereinigung aller Programme, Geräteobjekt speichergeführt (kein Schreibvorgang ohne Änderung), die drei `info.devices*`-Summen einmal je Durchgang gespült statt je Gerät, per-Gerät info.reachable (CONNECTED/DISCONNECTED/DEPAIRED, Übergänge als DEBUG-Log mit Label `Name (id)`; EINE „Setting up N appliance(s)"-Info-Zeile VOR der Geräte-Schleife, „New appliance"-Info nur bei Erstvergabe), Slug-Kollisions-Entdopplung, Re-Sync-Serialisierung, Options-Schreibgate (nur Optionen des GEWÄHLTEN Programms), top-level try/catch je fire-and-forget-Pfad **Seit 1.23.0 (Audit 2026-09-24):** Fehler je Gerät gefangen (die anderen Geräte laufen weiter, `seen` bleibt), Bestätigung einer Schreibung in normierter Form, `ProgramDef.keys` (Cache-Generation 3), CONNECTED während eines Durchgangs → genau ein weiterer (`resyncPending`), Erfolg räumt den Nicht-bereit-Neuversuch, kein Neuversuch für ein unerreichbares Gerät, veralteter REST-Wert verliert gegen einen jüngeren Stromwert (`lastStreamAt`/`passStartedAt`), wertloser Datensatz frischt keine Metadaten auf, abgebrochener Umzug wird fortgesetzt, Migration zählt den Zielkanal, Cache-Schlüssel füllen die Auswahlliste nur, solange sie noch keine hat, `stopped`-Wächter an jedem Schreibpfad, dauerhaft abgelehnte Definition 6 h gesperrt (nur 4xx außer 401/429; „beschäftigt“ bewusst nicht).
src/lib/device-catalog.ts       → Gerätetyp-Katalog (rein): Events je Typ (17 Typen, 49 Schlüssel — Quelle Ressourcen/homeconnect/device-type-catalog-2026-09-01.md), verriegelbare Türen, programlose Typen
src/lib/device-icons.ts         → Gerätetyp → Piktogramm: Karte für alle 17 Typen + `deviceIcon()`. Ergebnis geht als `common.icon` ans Geräteobjekt — **die Datei selbst als `data:image/svg+xml;base64`-URI** (einmal gelesen, gecacht; ~0,5 KB je Gerät), NICHT als Pfad: nur eine Inline-URI legt der Admin in den DOM, ein Pfad landet in einem nackten `<img>` und bleibt dort in seiner festen Farbe (1.19.0: schwarz auf schwarz in beiden Dark-Themes). Dateien in `admin/icons/` malen **ausschließlich in `currentColor`** (oder `none`) und erben so die Zeilenfarbe des Themes. Entscheidung 28
src/lib/oauth.ts                → OAuth Device Flow + Token-Refresh (rotierender Refresh-Token, OAuthError.oauthError trennt invalid_grant von transient; pollForToken liefert token/"pending"/"slow_down"; extractRefreshToken liest Alt-Klartext UND eigenes Format) **Seit 1.23.0:** `expires_in` auch als Zahl-String, sonst 86400 s (`lifetimeAssumed`, rotierter Refresh-Token bleibt); `needsRefresh`-Rand = min(1 h, max(Prüfintervall, halbe Laufzeit)); `interval` ≤ 0 → 5 s; 429 beim Poll → `slow_down`.
src/lib/http.ts                 → fetch-Transport: postForm (OAuth) + getJson/putJson/deleteJson über EINEN requestJson-Kern (Bearer, {data}-Envelope, BSH error.key, Retry-After auf 429); Antwortkörper gedeckelt (`MAX_RESPONSE_BYTES` 4 MiB, gestreamt gelesen und abgebrochen — „response too large“) **Seit 1.23.0:** ein abreißender Antwortkörper (Lesefehler NACH den Kopfzeilen) ist Status 0 wie jeder Transportfehler, in `postForm` wie in `requestJson` (F1).
src/lib/value-transformer.ts    → BSH-Enum → idiomatischer State (boolean/short-enum+states/number+unit+step); `common.name` = eigener Name aus `state-texts` (Ereignisse, schlägt alles) sonst lokalisierter Cloud-Name (`item.name`, Accept-Language) sonst `humanizeId` (Englisch aus der Kennung), `common.desc` = Erklärung aus `state-texts` (seit 2026-09-07 trägt JEDER Eintrag eine), `nameSource` api/derived/i18n; Programm-/Tür-/Läuft-Datenpunkte als Übersetzungsobjekte; `displayvalues` der Cloud schlagen die kuratierten Enum-Labels; Kind-Segment wird IRGENDWO im Schlüssel gefunden, Rest camelCase-verbunden (kein misc mehr); expandBshItem: DoorState→doorOpen(+doorLocked bei verriegelbaren Typen), Status.Door.*→door<Fach>Open, OperationState→zusätzlich programRunning — **ohne Wert kein Wert, an JEDER Expansion** (Audit 2026-09-15 F7: ein Datensatz ohne `value` machte Tür, Läuft-Schalter und Ereignis zu `false`; der Fallback-Pfad hielt die Regel seit 1.18.0, die Expansionen nicht); constraints.access="read" ⇒ write:false; transformOptionDefinition; parseConstraints (eine Boundary-Parse-Stelle) **Seit 1.23.0:** `shortEnumIn` (Programmlisten: kollidierende letzte Segmente → zwei Segmente, Nr. 35); Enum ohne Wert → kein Wert; Option mit `access: "read"` → `write:false`, ohne `default` kein erfundener Wert; `EventPresentState` ohne eigenen Schlüssel → `events.unnamedEvent`.
src/lib/command-dispatch.ts     → reine Abbildung State-Write → PUT/DELETE-Request (settings/commands/programs/options) **Seit 1.23.0:** `resolveEnum` exportiert (voll → listen-eindeutiger Kurzwert → eindeutiges nacktes Segment); Optionen gehen mit dem Schlüssel aus `ProgramDef.keys` des scharfen Programms raus, Wertfamilie über dessen Domäne (Nr. 34); ein mehrdeutiges nacktes Segment wird nicht gesendet (Nr. 35).
src/lib/event-stream.ts         → EINE persistente SSE-Verbindung (abgelehnter Verbindungsaufbau: Körper wird abgebrochen (undici hält ihn sonst bis zur GC), Grund mit wahrscheinlicher Ursache als Halbsatz — `refusedReason`: 5xx/404 Home-Connect-Seite, 401/403 Anmeldung abgelehnt, 429 Kontingent —, derselbe Text in `lastError` für den Verbindungstest; fetch-Stream, Verbindungsaufbau-Watchdog 30 s über verwalteten Timer, Keep-Alive-Watchdog, Backoff-Reconnect: Reset erst nach ≥60s stabiler Verbindung; Verbindungsfehler warn-einmal → debug → „connected again“-Info; 401 → `onUnauthorized` = Token-Refresh; **`reconnectNow()`** bricht einen anhängigen Backoff ab, wenn nach einer Neu-Anmeldung zur Laufzeit ein frischer Token da ist — nur bei anhängigem Timer, nie in eine laufende Verbindung hinein (zweiter Kanal); Audit 2026-09-15 §7.1: ohne Token wuchs der Backoff 10→300 s und der Token kam in einen laufenden 300-s-Timer) **Seit 1.23.0:** 429 → Reconnect nach max(Backoff, Retry-After bzw. 60 s) + `onRateLimited`; Keep-Alive-Fenster 130 s; abgelehnter Aufbau liest den Körper gedeckelt (5 s) für den BSH-`error.key`; eigene Texte für „connected without a body“ und „no answer within 30 s“.
src/lib/sse-parser.ts           → reiner inkrementeller SSE-Zeilen-Parser; Puffer-Deckel 1 MB (endloser Datenstrom ohne Zeilenumbruch/Leerzeile wird verworfen statt Speicher zu fressen) Seit 1.23.0 trennt er auch an einem einzelnen CR; ein CR am Chunk-Ende wartet auf den nächsten Chunk (CRLF über die Grenze).
src/lib/log-dedup.ts            → warn-once-per-Kategorie-dann-debug für REST-Fehler (auf status+error.key, kein String-Matching); **Schlüssel = Endpunkt-ART** (`restLogKey`: haId und Einstellungs-/Befehls-/Options-/Programmschlüssel → `*`, normalisiert IN der Klasse für Fehler, Erholung und Ratenpausen-Drop gleichermaßen — Audit 2026-09-15 F10: je vollem Pfad gab ein 503 beim Start 25 Warnungen und 25 „succeeded again"; die Logzeile nennt weiter den vollen Pfad)
src/lib/legacy-cleanup.ts       → reine Plan-Funktion (fakeroku-Muster): erkennt Alt-Generation-Bäume (haId-Wurzel mit Großbuchstaben ODER Unterstrich-BSH-Blätter) → main löscht sie rekursiv beim Start, VOR dem Priming; auth/info + eigene device-Bäume (native.haId) sind tabu
src/lib/pure-helpers.ts         → slugify (Umlaut-Transliteration + Unicode-Akzent-Strip) + disambiguateSlug + errMessage (**die Flotten-Masterform unverändert bis auf den Namen** — `CLAUDE_PATTERNS.md` § Async-Handler, Werkstatt 86d251b, seit 1.22.0: Error→Meldung, bei leerer Meldung der `code` (AggregateError von `localhost`), dazu GENAU eine Ebene `cause` ohne Doppelung — Node-`fetch` meldet jeden Netzfehler als `fetch failed`, der Grund steht nur dort; String→selbst, Primitive→`String()`, Objekt→`JSON.stringify` im `try` mit Rückfall auf den Typ-Tag. Master-Test unverändert in `err-message.test.ts` (echter `fetch` gegen geschlossenen Port/abreißenden Socket). Das Anmelde-Panel IMPORTIERT den Helfer aus `src/` — keine zweite Kopie (`error-text-helper`); `dts: false` am Module-Federation-Plugin, sonst legt dessen `tsc` eine `.d.ts` neben die Quelle) + API-Boundary-Guards (isRecord/numberOrUndef/stringArrayOrUndef) + `cleanLabel` (Cloud-Text → Objektname/Log: Steuerzeichen raus, Deckel 200) + `humanizeId` (camelCase → „Operation state“; einzelner Kleinbuchstabe vor einem Großwort bleibt zusammen: `iDos1Active` → „iDos 1 active“) + `coerceForType` (Schreibwert → `common.type`)
src/lib/state-texts.ts          → **Texttabelle: BSH-Schlüssel → eigener Name + verständliche Erklärung** (je ein i18n-Schlüssel, 11 Sprachen). 49 Ereignisse über alle 17 Gerätetypen + die häufigen Gerätedaten (Betriebszustand, Fernbedienung, Kindersicherung, Restzeit/Fortschritt, Programm-Wurzeln, Quittier-Befehl). **Jeder Datenpunkt bekommt eine Erklärung** (krobi 2026-09-07: „warum kannst du nicht ALLES übersetzen, egal ob das meine oder irgendwelche maschinen nutzen“) — wo der Hersteller nichts dokumentiert und der Wert kodiert ankommt, sagt der Satz genau das. Verboten bleibt allein die ERFUNDENE Bedeutung. **Nummerierte Familien** (`NUMBERED_FAMILIES`) decken `…Program.Details.ProgramNN` mit EINEM Eintrag ab; die Nummer füllt das `%s` der Texte über `StateText.args`. Die **Tür-Fachnamen** (`DOOR_COMPARTMENT_NAMES`) liegen ebenfalls hier — die Tür-Expansion selbst bleibt im Transformer (sie ist ein TYP-Sonderfall: ein Enum-Schlüssel wird zu einem oder zwei Booleans mit eigenen IDs), aber die TEXTE gehören dorthin, wo alle Texte stehen, sonst zählt eine Vollständigkeitsprüfung über `STATE_TEXTS` elf übersetzte Datenpunkte als unbenannt
src/lib/i18n.ts                 → `tName(key, ...args)` = adapter-core `I18n.getTranslatedObject` mit `I18nKey` aus `admin/i18n/en.json` (Tippfehler = Compile-Fehler)
src-admin/                      → React-Anmelde-Panel (Module-Federation, Admin-8-only, guiApi 2). Zeigt Verifizierungs-Link + Status live via Socket (`auth.verificationUrl`, `auth.signedIn`, `info.connection` → angemeldet+live / angemeldet ohne Live-Updates / Link / Hinweis; die drei Abonnements laufen in einem EIGENEN `try` nach den Erstlesungen — wirft eine Lesung, weil der Adapter noch nie lief, bleibt das Panel trotzdem live, Audit 2026-09-15 §7.4; `src-admin/tsconfig.json` ohne `baseUrl`, TS 6 lehnt es ab) + Knopf „Verbindung testen“ (sendTo `checkConnection`, Antworttext wörtlich). Build → admin/custom (git-getrackt). Vorbild public-holidays. Blaupause: Memory `reference_admin8_custom_component_migration` + `reference_iobroker_mf_custom_component`
```

## Design-Entscheidungen

_Jede Entscheidung steht hier als Regel-Satz; Beleg, Messung und Verlauf stehen wörtlich in `.claude/dev-history.md`, Eintrag „2026-09-21 — Design-Entscheidungen: Belege aus CLAUDE.md verlegt“ (lokal, gitignored)._

1. **Node-22-`fetch`** — statt axios — keine Extra-Dependency, kein manueller Timeout-Timer (`AbortSignal.timeout`).
2. **Eigener SSE-Parser** — statt `eventsource` — der Alt-Adapter hatte dort den Listener-Leak (unpassende Arrow-Refs bei add/remove).
3. **Token VERSCHLÜSSELT** — (`adapter.encrypt`/`decrypt` in `auth.session`) — der Alt-Adapter speicherte Klartext (Bug).
4. **Sprechende State-IDs** — `events.programFinished` statt `BSH_Common_Event_ProgramFinished`; Werte als boolean/short-enum/number statt roher BSH-Strings.
5. **Ein `applyBshItem`-Pfad** — für REST-Sync UND Stream — Programm-States (Root.ActiveProgram/SelectedProgram) laufen als synthetische Items durch denselben Pfad → REST + Live-Updates konvergieren, statt doppelte Objekte anzulegen.
6. **Stream setzt nur Werte, REST besitzt die Metadaten** — (`knownStates` + `metaSignature`) — Stream-Events legen ein Objekt höchstens neu an, nie um (→ keine Objektbaum-Flut des Alt-Adapters, #387).
7. **Optionen nur an `selected/options`** — Schreiben aufs aktive Programm ist geräte-zustandsabhängig (409 in den meisten Zuständen); ein Ziel ist vorhersagbar. **Alle Datenpunkte sofort:** beim Sync werden die Definitionen ALLER verfügbaren Programme geladen …
8. **Schreib-Bestätigung** — erfolgreicher non-button-Write wird mit `ack:true` bestätigt (selected/options sendet evtl. kein NOTIFY); momentane Buttons setzen sich nach dem Druck auf `false` zurück. **Eine ABGELEHNTE Schreibung liest die Ressource …
9. **`info.reachable` pro Gerät** — gespeist aus dem `connected`-Flag der Geräteliste + den CONNECTED/DISCONNECTED-Stream-Events. **`statusStates.onlineId` am Geräte-Objekt** (voller Pfad!) macht daraus das grün/graue Symbol im Objektbaum — der Wert …
10. **Der Adapter räumt beim Update selbst auf** — (krobi 2026-08-18: „der Adapter muss das aufräumen, nicht der User"): die Alt-Generation-Bäume (rohe haId-Wurzeln) werden beim Start automatisch entfernt (`legacy-cleanup`), der Login wird übernommen.
11. **Summen-Datenpunkte** — (seit v1.11.0) — `info.devicesTotal`/`devicesOnline`/`devicesAllOnline`, abgeleitet in `setReachable()`, der EINEN Stelle, durch die jeder Marker-Schreibvorgang läuft; eine zweite Rechenstelle würde driften.
12. **Datenpunkt-Namen und -Beschreibungen** — (seit v1.14.0, krobi 2026-09-02: „da sind quasi keine Beschreibungen, sondern einfach der Datenpunkt als Beschreibung“).
13. **`info.connection` hat EINEN Schreiber** — (seit v1.14.0): `main` hält `signedIn` (Anmeldung) und `streamUp` (Ereignisstrom) und schreibt das UND.
14. **Schreibpfad typisiert** — (seit v1.14.0): `coerceForType` bringt den geschriebenen Wert in den `common.type` des Datenpunkts (`"true"`/`1` → `true`, `"40"` → `40`); was sich nicht wandeln lässt, wird gar nicht erst gesendet (debug).
15. **Cloud-Text wird gesäubert** — (seit v1.14.0): `cleanLabel` (Steuerzeichen → Leerzeichen, Deckel 200) für App-Namen, Optionen-/Befehls-Namen und jeden Text, der Objektname oder Log-Label wird; in `native` landen nur Strings …
16. **Transport-Härtung** — (seit v1.14.0): HTTP-Antwortkörper gedeckelt (4 MiB, gestreamt gelesen, darüber abgebrochen); Ereignisstrom mit Verbindungsaufbau-Watchdog (30 s, verwalteter Timer — der Keep-Alive-Wächter existiert erst, wenn der …
17. **Der Verbindungstest prüft wirklich** — (seit v1.14.0, krobi: „ein Verbindungstest muss real testen, echtes Feedback, kein jaja passt schon“): `checkConnection` (sendTo aus dem Panel, `messagebox: true`) macht eine ECHTE Anfrage `GET /api/homeappliances` mit …
18. **Die Namensvergabe erreicht auch BESTEHENDE Bäume** — (seit v1.15.0 — der Fund beim Server-Test von v1.14.0, an krobis drei Geräten gemessen): der REST-Abgleich fasst nur an, was das Gerät in DIESER Antwort meldet.

19. **Die eigenen Manifest-Objekte benennt der Adapter selbst** — (seit dem Audit 2026-09-04). js-controller wendet die `instanceObjects` bei jedem Start an, aber mit `preserve: { common: ["name"] }` — die Beschreibung kommt im Bestand an, eine **Umbenennung** erreicht nur Neuanlagen.

20. **Nach einem Strom-Reconnect wird nachgezogen** — (seit dem Audit 2026-09-07).

21. **Ein am GERÄT gewähltes Programm bewaffnet das Options-Gatter neu** — (seit dem Audit 2026-09-07).

22. **Ein rotierender Token, der nicht gespeichert werden konnte, wird als das gemeldet, was er ist** — (seit dem Audit 2026-09-07).

23. **Ein fehlgeschlagener Etiketten-Schreibvorgang gilt nicht als erledigt** — (seit dem Audit 2026-09-07) — dieselbe Regel, die Nr. 6 für die Metadaten trägt.

24. **Die Erklärung eines Befehls hängt am BSH-Schlüssel, nicht am Weg des Namens** — (seit dem Audit 2026-09-07).

25. **Eigene Namen auch für Status und Einstellungen** — (seit dem Audit 2026-09-07; Rangfolge am 2026-09-12 umgekehrt, siehe Nr. 12).
26. **Zusatz-Datenpunkte des erweiterten Home-Connect-Zugriffs** — (seit 1.17.1). Wer im Developer-Portal die zusätzlichen Gerätedaten einschaltet, bekommt Schlüssel, die **keine** Quelle dokumentiert: weder `api-docs.home-connect.com` noch die 1020-Schlüssel-Referenz von …

27. **Die Metadaten einer Einstellung stehen im EINZEL-Endpunkt** — (seit dem Audit 2026-09-12, an krobis laufender Anlage gemessen).
28. **Jedes Gerät trägt ein Piktogramm seines Typs** — (v1.19.0; Form korrigiert danach, siehe (b)).
29. **Gerätezustände sind Antworten, keine Fehler, und räumen den Recovery-Schlüssel** — (seit 1.22.0, Log-Audit 2026-09-23). Keins/beschäftigt/nicht unterstützt warnen nie und beweisen den Endpunkt; der Preis des haId-freien Schlüssels (Audit 09-15 F10) bleibt bewusst: ein Gerät im Leerlauf räumt die Art, während ein anderes darauf scheitert — ein warn + ein „succeeded again“ je Durchgang, per Test festgenagelt.
30. **Nach „nicht bereit“ liest der Adapter selbst nach** — (seit 1.22.0): 30/60/120 s, dann wartet er auf das nächste CONNECTED; alles auf debug (krobi 2026-09-23: Aufgeben auf debug).
31. **Nicht-API-Programme bleiben außerhalb `common.states`** — (seit 1.22.0): ein am Gerät gewähltes Programm, das die API nicht anbietet, steht als Rohwert (`auto30`) in `selectedProgram`; in die Auswahlliste gehört es nicht, denn schreiben lässt es sich nicht.
32. **Die Startkette startet Abo und Ereignisstrom immer** — (seit 1.23.0, Audit 2026-09-24 F1/F2): ein gescheiterter Geräte-Abgleich (auch ein abgerissener Antwortkörper, den der Transport seitdem als Status 0 meldet) kostet weder Schreibpfad noch Live-Updates; ein Gerät, das nicht eingerichtet werden kann, kostet die anderen nichts.
33. **Die lokalen Startschritte laufen einmal, in `onReady`** — (seit 1.23.0, F5/F13): Migrationen, Priming und Erreichbarkeits-Stempel vor Zugangsdaten-Prüfung und Anmeldung; der Anmelde-Rückruf (auch jede Neu-Anmeldung zur Laufzeit) liest nur die Geräte, abonniert und öffnet den Strom. `auth.signedIn` fällt beim Start wie beim Stopp auf false.
34. **Eine Option geht mit dem Schlüssel des scharfen Programms raus** — (seit 1.23.0, F6): zwei Gerätefamilien benennen dieselbe Option verschieden und teilen eine State-ID; der Programm-Definitionscache (Generation 3) merkt je Programm den Schlüssel, der Wert wird in dessen Wertfamilie aufgelöst. Die Option behält EINEN Kurzwert.
35. **Programmlisten tragen listen-eindeutige Kurzwerte** — (seit 1.23.0, F7): enden zwei Programme einer Liste auf dasselbe Segment, bekommen beide die letzten zwei Segmente (`heatingmode.doughproving`); ohne Kollision bleibt der Wert wie bisher. Das nackte Segment ist dann mehrdeutig und wird nicht gesendet (warn mit den gültigen Werten). Eine Schreibung wird in dieser Form bestätigt, nie wörtlich (F4).

## Wertekonvertierung (value-transformer)

- **Event** (`.Event.`, EventPresentState) → boolean (`Present` → `true`). **Fehlt der Wert (oder ist `null`), wird KEIN Wert erzeugt** — das gilt für Ereignis, Tür-Booleans und `programRunning` genauso wie für den Fallback (2026-09-15).
- **Enum** (`.EnumType.`/`.Program.`) → short-Wert (`…OperationState.Run` → `"run"`) + `common.states`.
- **Zahl** → number + `unit` + `min`/`max`/`step` aus Constraints.
- **`constraints.access === "read"`** ⇒ `write:false`, auch im settings-Kanal (die API kennt Nur-Lese-Settings).
- **Schreibbare Enums** (settings, selectedProgram, Optionen) tragen die vollen BSH-Werte in `native.bshValues` — `shortEnum` ist verlustbehaftet, der Schreibpfad löst den short-Wert darüber zurück auf. **Schreibweise tolerant** (seit 1.20.0): `on`, `On`, `ON` und der volle BSH-Wert lösen gleich auf; ein Wert, den das Gerät nicht anbietet, wird weiterhin nicht gesendet.
- **Enum-Labels (`common.states`):** `displayvalues` der Cloud (lokalisiert) > kuratierte Common-Enums (`OperationState`, `PowerState`) > Kurzwert als eigenes Label. Ausschließlich Plain-String-Maps (Admin-Rendering).

## Quirks / Always-Rules

- **`onUnload`:** Timer und Kollaborateure synchron abräumen, dann `info.connection` + alle Geräte-Marker schreiben und **`callback()` erst danach** (`.finally`) — der Host gewährt den vollen `stopTimeout`, ein vorher gefeuerter Rückruf verliert die Schreibvorgänge.
- **Async-Handler** `.bind(this)` + top-level `try/catch` im Body.
- **Rate-Limit (amtlich, api-docs → Rate Limiting):** 1000 Requests/Tag + 10/s (20 Burst; dieser 429 kommt OHNE Retry-After → 60-s-Fallback-Pause; der Transport hält die 10/s selbst ein: 100 ms Mindestabstand je Anfrage, Nr. 27) + **Token-Endpunkt eigenes Kontingent 10/min + 100/Tag** (→ Refresh-Fehlversuche mit wachsendem Backoff 30 s→30 min). Kein Polling-Sturm; der Event-Stream ist der Update-Pfad (zählt 1 Request, Stream-Nachrichten zählen nicht), ein `CONNECTED`/`PAIRED`-Event triggert den Re-Sync eines Geräts (kein Poll-Intervall).
- **API-Boundary-Type-Guards** — `isRecord`/`numberOrUndef`/`stringArrayOrUndef` vor Zugriff auf externe Daten.
- **`npm run format` schließt per Negativ-Muster aus, was ein WERKZEUG schreibt** — `build/` (esbuild), `admin/custom/` (Module-Federation-Bau), `io-package.json` (Release-Skript + `sync-iopackage-from-i18n.py`), `test/objects.inventory.json` (der Inventarlauf, `JSON.stringify`), `README.md` + `CHANGELOG_OLD.md` (Changelog-Plugin), `.releaseconfig.json` (Konsistenz-Master), vier einzelne `.github/`-Dateien (`dependabot.yml`, `FUNDING.yml`, `workflows/automerge-dependabot.yml`, `copilot-instructions.md`), `.vscode/`, `src-admin/package*.json` und die beiden `tsconfig`-Dateien — maßgeblich ist die Musterliste in `package.json`. Jede dieser Dateien hat ihren eigenen Erzeuger und ihre eigene Prüfung — prettier ist nicht ihre Autorität, und ein Handformat wäre beim nächsten Werkzeuglauf ohnehin wieder weg. **KEINE `.prettierignore` anlegen:** der Repo-Prüfer meldet sie bei `@iobroker/eslint-config` als überflüssig (W0084 + W5048, am 2026-09-04 gemessen) — die Eingrenzung gehört in den Befehl, nicht in eine Ignorier-Datei. `docs/**`, die übrigen `.github/`-Dateien (Workflow, Issue-Vorlagen) und `package.json` werden formatiert (Flotten-Fassung seit 2026-09-08).

## Tests

vitest, Tests neben Source unter `src/**/*.test.ts` — die reinen Bausteine (oauth, http, value-transformer, command-dispatch, sse-parser, log-dedup, pure-helpers, device-catalog, device-icons, legacy-cleanup, dazu der Master-Test des Fehlertext-Helfers `err-message.test.ts`), die beiden extrahierten Orchestratoren (auth-controller, appliance-sync — je über Fake-Port + injizierte Timer/Uhr) UND `main.ts` (Orchestrationstests mit Attrappen der drei Kollaborateure über die Konstruktions-Nahtstellen: Anmeldung, REST-Transport, Ratenpause, `info.connection`, Abbau). Externe API-Werte werden an der Grenze type-guarded. Test-Audits per Mutationstest: `Ressourcen/homeconnect/test-audit-2026-08-22.md` (133) + `test-audit-2026-09-02.md`; In-depth-Audits + Mutationsaudit der Fixes: `audit-2026-09-04.md` (9/9 Mutationen gefangen) und `audit-2026-09-07.md`. Forensisches Audit 2026-09-15 (F1–F10, umgesetzt in 1.20.0): `audit-2026-09-15.md`, Tabelle `mutations_homeconnect_2026-09-15.py` 25/25. Log-Audit 2026-09-23 → 1.22.0 (`audit-2026-09-23.md`); Code-Audit 2026-09-24 (fünf Prüfer, F1–F27) → 1.23.0 (`audit-2026-09-24.md`, Tabelle `mutations_homeconnect_2026-09-24.py`). **Anmelde-Panel mit eigener Suite** (seit 1.23.0): `src-admin/src/SignIn.test.tsx`, vitest + jsdom gegen die echte `ConfigGeneric` im src-admin-Paket; `npm run test:admin` = `check:admin` + diese Suite, CI-Job `admin-component`. Die Nadeln auf `src-admin/src/` laufen mit dieser Suite (das Mutationswerkzeug erkennt das Unterprojekt an `package.json` + `vitest.config.ts`). ⚠️ Der Inventarlauf dauert seit 1.20.0 ~2 min: gegen den sofort antwortenden Fixture-Server IST der 100-ms-Anfrageabstand die Laufzeit (Frist in `test/inventory.js` 300 s). **Objekt-Inventar** (`npm run test:inventory`): `test/inventory.js` startet den Adapter im Wegwerf-Controller und treibt ihn über `test/fixtures/inventory/<Typ>.json` — **alle 17 Gerätetypen**, aus der wörtlichen Typquelle `Ressourcen/homeconnect/upstream-refs/api-value-types.ts` abgeleitet, nicht nur krobis drei Geräte. Weil der Adapter ein reiner Cloud-Client ist, erreichen die Fixtures ihn über die UMGEBUNG: `NODE_OPTIONS=--require test/inventory-fetch-hook.cjs` ersetzt im Adapterprozess `globalThis.fetch` durch eine Routing-Tabelle (OAuth-Gerätefluss, Geräteliste, die vier Geräte-Ressourcen, Programm-Definitionen und der SSE-Strom) und **weist jede unbekannte Adresse ab** — kein Aufruf verlässt die Maschine, der Adapter hat keine Test-Naht. Warte-Kriterium ist ein ZUSTAND (`info.devicesTotal == 17`), nie „der Baum wächst nicht mehr". ⚠️ Der Lauf BAUT NICHT — `npm run build` gehört von Hand davor. ⚠️ Das Inventar wird wie in der Flottenvorlage mit `JSON.stringify` geschrieben und ist deshalb aus `format`/`format:check` ausgeschlossen (siehe oben) — prettier setzt ein einelementiges `native.bshValues` einzeilig, `JSON.stringify` bricht es um; beurteilt wird die Datei von `check-object-inventory.py`, das sie parst, nicht von prettier. Suiten, die `value-transformer` laden, brauchen die adapter-core-Attrappe (I18n) — das echte Modul beendet den Prozess außerhalb eines Adapters.

## Versionshistorie

Aktuelle Version: `io-package.json`. User-facing Changelog: `README.md` + `io-package.json:common.news` (11 Sprachen, handgeschrieben). Interne Entwicklungs-Historie: `.claude/dev-history.md` (lokal, nicht git-getrackt).

## Befehle

```bash
npm run build            # Production-Build (esbuild via build-adapter)
npm run check            # tsc --noEmit (Type-Check ohne Build)
npm run test:ts          # Unit-Tests via vitest
npm test                 # test:ts + test:package (lokal)
npm run test:integration # Boot-Test (Adapter startet real)
npm run test:admin       # Anmelde-Panel: Typprüfung + eigene vitest-Suite (braucht vorher build:admin)
npm run lint             # ESLint
npm run format           # Prettier — alles außer den Werkzeug-Ausgaben (Musterliste in package.json, siehe Quirks)
npm run coverage         # Abdeckung (der Bericht liegt in coverage/ und ist git-ignoriert)
```
